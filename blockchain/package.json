{
  "name": "charitylake-blockchain",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy:localhost": "hardhat run --network localhost scripts/deploy.js",
    "deploy:sepolia": "hardhat run --network sepolia scripts/deploy.js"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.0",
    "@openzeppelin/contracts": "^4.9.0",
    "dotenv": "^16.0.0",
    "ethers": "^6.15.0",
    "hardhat": "^2.16.0"
  }
}
